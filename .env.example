# Omi API Configuration (Required)
# Generate your API key from the Omi app: Settings → Developer → Create Key
OMI_API_KEY=omi_dev_xxxxxxxxxx

# WebDAV Server Configuration (Required)
WEBDAV_URL=https://your-webdav-server.com
WEBDAV_USER=your_username
WEBDAV_PASS=your_password

# Optional Configuration
# Remote directory path for markdown files (default: /conversations)
# OUTPUT_DIR=/conversations

# Seconds between sync runs (default: 300 = 5 minutes)
# SYNC_INTERVAL_SECONDS=300

# Timezone for logging (default: UTC)
# TZ=UTC
